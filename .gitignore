# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Electron build output (Phase 3)
release

# Recorded validation sessions (engine-validation) — data, not source
captures/*.json
captures/*.json.tmp

# Sync server data (v2) — uploaded captures, not source
sync-data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cygwin/MSYS crash dumps from the bundled bash
*.stackdump

# Race snapshots. This repo is PUBLIC and a snapshot carries driver names,
# a LAN address and the race setup (tyre lives and lap times). Carry them
# between machines by hand, not through git. See saves/README.md.
saves/*.json
