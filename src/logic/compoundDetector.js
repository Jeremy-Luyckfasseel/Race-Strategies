/**
 * GT7 telemetry does not expose tire compound ID — it is not in the UDP packet.
 * Compound identification requires user input after each pit stop.
 * See useCompoundDetector.js for the pit-exit detection hook.
 */
